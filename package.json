{
  "name": "rakshak-live",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "agent",
    "packages/*"
  ],
  "scripts": {
    "client": "npm run dev --workspace=client",
    "server": "npm run dev --workspace=server",
    "agent": "npm run start --workspace=agent",
    "dev": "concurrently \"npm run client\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
